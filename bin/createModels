#!/bin/bash
[ -z $LOD_ANALYSIS_JAR ] && echo "LOD_ANALYSIS_JAR environment variable not set" && exit 1;

force='-force';


java -jar $LOD_ANALYSIS_JAR $force -metrics $METRIC_DIR "lodanalysis.model.CreateModels";
